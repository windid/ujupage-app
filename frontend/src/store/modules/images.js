import * as types from '../mutation-types'
// import Vue from 'vue'

const state = {
  all: [],
  current: {}
}

const mutations = {
  [types.LOAD_IMAGES] (state, { images }) {

  }
}

export default {
  state,
  mutations
}
